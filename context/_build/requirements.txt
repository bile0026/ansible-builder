# Python 3.12 runtime. The Stream 9 base has none of the packages the
# awx-ee 24.6.1 image provided, so carry forward the ones network jobs use.
netmiko>=4.7,<4.8
ansible-pylibssh>=1.4,<1.5
# netmiko requires paramiko <5; 4.x drops DSA (ssh-dss) key support
paramiko>=3.5,<5
ncclient>=0.7,<0.8
jmespath>=1.0,<2
pexpect>=4.9,<5
